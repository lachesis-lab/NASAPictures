<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/item_settings"
        android:icon="@drawable/ic_settings_menu"
        app:showAsAction="always"
        android:title="@string/item_settings_title" />
    <item android:id="@+id/animation_item"
        android:title="@string/animation_title"/>
    <item android:id="@+id/notes_item"
        android:title="@string/notes_title"
        android:icon="@android:drawable/ic_menu_edit"
        app:showAsAction="ifRoom"/>

</menu>